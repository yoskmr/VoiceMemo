# TASK-0013: メモテキスト編集

> **タスクID**: TASK-0013
> **フェーズ**: Phase 2 - メモ管理 + 検索
> **タイプ**: TDD
> **見積工数**: 4h
> **依存タスク**: TASK-0012
> **関連要件**: REQ-016
> **ステータス**: TODO
> **担当モジュール**: FeatureMemo

---

## 1. 概要

文字起こしテキストの編集・保存機能を実装する。SwiftUIのTextEditorを使用し、変更検出と自動保存（デバウンス）を行う。ユーザーがテキストを手動修正した場合にSwiftDataとFTS5インデックスの両方を更新する。

---

## 2. 完了条件（Definition of Done）

- [ ] MemoEditReducer のユニットテスト（TestStore）が全てパス
- [ ] TextEditorで文字起こしテキストが編集可能
- [ ] タイトルも編集可能
- [ ] 変更検出: 元テキストとの差分がある場合のみ保存ボタンが有効化
- [ ] 自動保存: テキスト変更後2秒間入力がなければ自動保存（デバウンス）
- [ ] 手動保存: 保存ボタンタップで即座に保存
- [ ] 保存完了時にFTS5インデックスも更新される
- [ ] 戻るボタン押下時に未保存変更がある場合、確認ダイアログ表示
- [ ] VoiceOverアクセシビリティ対応が完了している

---

## 3. 技術仕様

### 3.1 TCA State / Action / Reducer

```swift
// FeatureMemo/MemoEdit/MemoEditReducer.swift
import ComposableArchitecture
import Foundation

@Reducer
struct MemoEditReducer {
    @ObservableState
    struct State: Equatable {
        let memoID: UUID
        var title: String = ""
        var transcriptionText: String = ""

        // 変更検出用の元テキスト
        var originalTitle: String = ""
        var originalTranscriptionText: String = ""

        // UI状態
        var isSaving: Bool = false
        var hasUnsavedChanges: Bool = false
        var showDiscardAlert: Bool = false
        var saveSuccessMessage: String?
        var errorMessage: String?

        var isModified: Bool {
            title != originalTitle || transcriptionText != originalTranscriptionText
        }
    }

    enum Action: BindableAction {
        case binding(BindingAction<State>)
        case onAppear(title: String, transcriptionText: String)
        case titleChanged(String)
        case transcriptionTextChanged(String)
        case saveButtonTapped
        case autoSaveTriggered
        case saveCompleted(Result<Void, Error>)
        case backButtonTapped
        case discardConfirmed
        case discardCancelled
        case dismissSaveSuccess
    }

    @Dependency(\.memoRepository) var memoRepository
    @Dependency(\.fts5IndexManager) var fts5IndexManager
    @Dependency(\.continuousClock) var clock

    private enum AutoSaveID { case debounce }

    var body: some ReducerOf<Self> {
        BindingReducer()

        Reduce { state, action in
            switch action {
            case let .onAppear(title, transcriptionText):
                state.title = title
                state.transcriptionText = transcriptionText
                state.originalTitle = title
                state.originalTranscriptionText = transcriptionText
                return .none

            case let .titleChanged(newTitle):
                state.title = newTitle
                state.hasUnsavedChanges = state.isModified
                return .run { send in
                    try await clock.sleep(for: .seconds(2))
                    await send(.autoSaveTriggered)
                }
                .cancellable(id: AutoSaveID.debounce, cancelInFlight: true)

            case let .transcriptionTextChanged(newText):
                state.transcriptionText = newText
                state.hasUnsavedChanges = state.isModified
                return .run { send in
                    try await clock.sleep(for: .seconds(2))
                    await send(.autoSaveTriggered)
                }
                .cancellable(id: AutoSaveID.debounce, cancelInFlight: true)

            case .saveButtonTapped, .autoSaveTriggered:
                guard state.isModified, !state.isSaving else { return .none }
                state.isSaving = true
                let memoID = state.memoID
                let title = state.title
                let text = state.transcriptionText
                return .run { send in
                    let result = await Result {
                        try await memoRepository.updateMemoText(
                            id: memoID,
                            title: title,
                            transcriptionText: text
                        )
                        // FTS5インデックスも更新
                        try await fts5IndexManager.upsertIndex(
                            memoID: memoID.uuidString,
                            title: title,
                            transcriptionText: text,
                            summaryText: "",  // 既存の要約は変更しない
                            tags: ""
                        )
                    }
                    await send(.saveCompleted(result))
                }

            case .saveCompleted(.success):
                state.isSaving = false
                state.originalTitle = state.title
                state.originalTranscriptionText = state.transcriptionText
                state.hasUnsavedChanges = false
                state.saveSuccessMessage = "保存しました"
                return .run { send in
                    try await clock.sleep(for: .seconds(2))
                    await send(.dismissSaveSuccess)
                }

            case let .saveCompleted(.failure(error)):
                state.isSaving = false
                state.errorMessage = error.localizedDescription
                return .none

            case .backButtonTapped:
                if state.hasUnsavedChanges {
                    state.showDiscardAlert = true
                    return .none
                }
                // 親Reducerで画面遷移を処理
                return .none

            case .discardConfirmed:
                state.showDiscardAlert = false
                state.hasUnsavedChanges = false
                // 親Reducerで画面遷移を処理
                return .cancel(id: AutoSaveID.debounce)

            case .discardCancelled:
                state.showDiscardAlert = false
                return .none

            case .dismissSaveSuccess:
                state.saveSuccessMessage = nil
                return .none

            case .binding:
                return .none
            }
        }
    }
}
```

### 3.2 MemoEditView（SwiftUI）

```swift
// FeatureMemo/MemoEdit/MemoEditView.swift

struct MemoEditView: View {
    @Bindable var store: StoreOf<MemoEditReducer>
    @FocusState private var focusedField: Field?

    enum Field: Hashable {
        case title
        case transcription
    }

    var body: some View {
        VStack(spacing: 0) {
            // 保存成功メッセージ
            if let message = store.saveSuccessMessage {
                HStack {
                    Image(systemName: "checkmark.circle.fill")
                        .foregroundColor(.vmSuccess)
                    Text(message)
                        .font(.vmCaption1)
                        .foregroundColor(.vmSuccess)
                }
                .padding(8)
                .frame(maxWidth: .infinity)
                .background(Color.vmSuccess.opacity(0.1))
                .transition(.move(edge: .top).combined(with: .opacity))
            }

            ScrollView {
                VStack(alignment: .leading, spacing: 16) {
                    // タイトル編集
                    VStack(alignment: .leading, spacing: 4) {
                        Text("タイトル")
                            .font(.vmSubheadline)
                            .foregroundColor(.vmTextSecondary)
                        TextField("タイトルを入力...", text: $store.title.sending(\.titleChanged))
                            .font(.vmTitle2)
                            .foregroundColor(.vmTextPrimary)
                            .focused($focusedField, equals: .title)
                    }

                    Divider()
                        .background(Color.vmDivider)

                    // 文字起こしテキスト編集
                    VStack(alignment: .leading, spacing: 4) {
                        Text("文字起こし")
                            .font(.vmSubheadline)
                            .foregroundColor(.vmTextSecondary)
                        TextEditor(text: $store.transcriptionText.sending(\.transcriptionTextChanged))
                            .font(.vmBody())
                            .foregroundColor(.vmTextPrimary)
                            .frame(minHeight: 300)
                            .focused($focusedField, equals: .transcription)
                            .scrollContentBackground(.hidden)
                    }
                }
                .padding(16)
            }
            .background(Color.vmBackground)
        }
        .navigationTitle("編集")
        .navigationBarTitleDisplayMode(.inline)
        .toolbar {
            ToolbarItem(placement: .topBarTrailing) {
                Button {
                    store.send(.saveButtonTapped)
                } label: {
                    if store.isSaving {
                        ProgressView()
                    } else {
                        Text("保存")
                            .font(.vmHeadline)
                            .foregroundColor(store.hasUnsavedChanges ? .vmPrimary : .vmTextTertiary)
                    }
                }
                .disabled(!store.hasUnsavedChanges || store.isSaving)
            }

            ToolbarItem(placement: .keyboard) {
                Button("完了") { focusedField = nil }
            }
        }
        .alert("未保存の変更があります", isPresented: $store.showDiscardAlert) {
            Button("変更を破棄", role: .destructive) {
                store.send(.discardConfirmed)
            }
            Button("編集を続ける", role: .cancel) {
                store.send(.discardCancelled)
            }
        } message: {
            Text("保存せずに戻ると、変更内容が失われます。")
        }
        .interactiveDismissDisabled(store.hasUnsavedChanges)
    }
}
```

---

## 4. TDDテスト計画

### 4.1 Red-Green-Refactor サイクル

| # | テストケース | 検証内容 |
|:--|:------------|:---------|
| 1 | `test_onAppear_初期値が設定される` | onAppear→title/transcriptionText/original値が正しく設定 |
| 2 | `test_titleChanged_変更検出` | titleChanged→hasUnsavedChanges=true、isModified=true |
| 3 | `test_transcriptionTextChanged_変更検出` | 変更→hasUnsavedChanges=true |
| 4 | `test_titleChanged_元に戻すと未変更` | 変更→元に戻す→hasUnsavedChanges=false |
| 5 | `test_autoSave_2秒デバウンス後に保存` | テキスト変更→2秒経過→autoSaveTriggered→saveCompleted |
| 6 | `test_autoSave_連続入力でデバウンスリセット` | 変更→1秒後に再変更→さらに2秒後に1回だけ保存 |
| 7 | `test_saveButtonTapped_即座に保存` | saveButtonTapped→isSaving=true→saveCompleted→hasUnsavedChanges=false |
| 8 | `test_saveCompleted_originalが更新される` | 保存後にoriginalTitle/originalTranscriptionTextが現在値に更新 |
| 9 | `test_saveCompleted_failure_エラー表示` | 保存失敗→errorMessage設定、isSaving=false |
| 10 | `test_backButton_未保存変更ありでアラート表示` | hasUnsavedChanges=true→backButtonTapped→showDiscardAlert=true |
| 11 | `test_backButton_未保存変更なしでそのまま戻る` | hasUnsavedChanges=false→backButtonTapped→showDiscardAlert=false |
| 12 | `test_discardConfirmed_変更破棄` | discardConfirmed→hasUnsavedChanges=false、autoSaveキャンセル |

### 4.2 TestStore テスト例

```swift
@MainActor
func test_autoSave_2秒デバウンス後に保存() async {
    let memoID = UUID()
    let testClock = TestClock()

    let store = TestStore(
        initialState: MemoEditReducer.State(memoID: memoID)
    ) {
        MemoEditReducer()
    } withDependencies: {
        $0.continuousClock = testClock
        $0.memoRepository.updateMemoText = { _, _, _ in }
        $0.fts5IndexManager.upsertIndex = { _, _, _, _, _ in }
    }

    await store.send(.onAppear(title: "元タイトル", transcriptionText: "元テキスト")) {
        $0.title = "元タイトル"
        $0.transcriptionText = "元テキスト"
        $0.originalTitle = "元タイトル"
        $0.originalTranscriptionText = "元テキスト"
    }

    await store.send(.transcriptionTextChanged("編集後テキスト")) {
        $0.transcriptionText = "編集後テキスト"
        $0.hasUnsavedChanges = true
    }

    await testClock.advance(by: .seconds(2))

    await store.receive(.autoSaveTriggered) {
        $0.isSaving = true
    }

    await store.receive(.saveCompleted(.success(()))) {
        $0.isSaving = false
        $0.originalTranscriptionText = "編集後テキスト"
        $0.hasUnsavedChanges = false
        $0.saveSuccessMessage = "保存しました"
    }
}
```

---

## 5. 依存関係

### 5.1 前提タスク

| タスクID | 内容 | 必要な成果物 |
|:---------|:-----|:------------|
| TASK-0012 | メモ詳細画面 | editButtonTapped → MemoEditView への遷移 |

### 5.2 後続タスク

なし（独立した編集機能）

### 5.3 @Dependency

| 依存名 | プロトコル | 提供モジュール |
|:-------|:----------|:--------------|
| memoRepository | MemoRepositoryProtocol | Domain / Data |
| fts5IndexManager | FTS5IndexManagerProtocol | InfraStorage |
| continuousClock | ContinuousClock | ComposableArchitecture (built-in) |

---

## 6. 実装手順

1. **MemoEditReducer の State / Action 定義**
2. **TestStoreテスト: onAppear / titleChanged / 変更検出** を記述（Red）
3. **Reducer body 実装**: onAppear, titleChanged, transcriptionTextChanged（Green）
4. **自動保存デバウンスのテスト・実装**（TestClock使用）
5. **saveButtonTapped / saveCompleted のテスト・実装**
6. **戻るボタン + 確認ダイアログのテスト・実装**
7. **MemoEditView のSwiftUI実装**
8. **FTS5インデックス更新の統合テスト**
9. **アクセシビリティ確認**

---

## 7. 設計書参照

| 設計書 | セクション | 内容 |
|:-------|:----------|:-----|
| 01-system-architecture.md | 5.2 SwiftData @Model | Transcription.fullText のフィールド定義 |
| 01-system-architecture.md | 5.3 SQLite FTS5 | upsertIndex による全文検索インデックス更新 |
| 04-ui-design-system.md | 3.1 フォントファミリー | TextEditor用フォント（HiraMaruProN-W4） |
| 04-ui-design-system.md | 6.3 メモ詳細画面 | 編集ボタンの配置位置 |
