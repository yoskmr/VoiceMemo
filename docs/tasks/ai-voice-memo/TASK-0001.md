# TASK-0001: Xcodeプロジェクト初期構築

**タスクID**: TASK-0001
**タスクタイプ**: DIRECT
**推定工数**: 8h
**フェーズ**: Phase 1 - 基盤構築 + 録音 + STT
**信頼性レベル**: :large_blue_circle: *設計文書01-system-architecture.md, 00-integration-spec.md準拠*

## 関連文書
- **概要**: [overview.md](overview.md)
- **要件定義**: [requirements.md](../../spec/ai-voice-memo/requirements.md) - REQ-008, NFR-012
- **設計文書**: [01-system-architecture.md](../../spec/ai-voice-memo/design/01-system-architecture.md) - セクション2, 3
- **統合仕様書**: [00-integration-spec.md](../../spec/ai-voice-memo/design/00-integration-spec.md) - セクション9(命名規則)

## タスク概要

Xcodeプロジェクトを新規作成し、SPMマルチモジュール構成（14モジュール）を構築する。TCA（The Composable Architecture）を導入し、SwiftLint設定、.gitignore等の開発基盤を整備する。全後続タスクの土台となるため、モジュール間の依存関係を設計書通りに正確に定義する。

## 依存タスク
- **前提**: なし（最初のタスク）
- **後続**: TASK-0002, TASK-0003, TASK-0005, TASK-0006, TASK-0008, TASK-0010

## 完了条件
- [ ] Xcodeプロジェクトが作成され、iOS 17.0以上をターゲットとしている
- [ ] SPMで14モジュール（App/Feature x6/Domain/Data/Infra x4/Shared x2）の全ターゲットが作成済み
- [ ] TCA（swift-composable-architecture 1.17+）がSPM経由で導入済み
- [ ] 各モジュール間の依存関係が設計書通り（Feature -> Domain, SharedUI / Infra -> Domain等）
- [ ] Feature -> Infra 直接依存が排除されている（統合仕様書H2準拠）
- [ ] SwiftLintが設定され、ビルド時に実行される
- [ ] .gitignoreが適切に設定されている
- [ ] `xcodebuild build` でビルドが成功する
- [ ] TCAのStoreが動作する最小限のAppReducerが実装済み

## 実装詳細

### 1. Xcodeプロジェクト作成 :large_blue_circle:

```
VoiceMemo/
├── VoiceMemo.xcodeproj
├── VoiceMemoApp/           # アプリターゲット
│   ├── VoiceMemoApp.swift  # @main エントリポイント
│   └── Info.plist
├── VoiceMemoModules/       # SPMパッケージ
│   ├── Package.swift
│   └── Sources/
│       ├── Domain/
│       ├── Data/
│       ├── FeatureRecording/
│       ├── FeatureMemo/
│       ├── FeatureAI/
│       ├── FeatureSearch/
│       ├── FeatureSettings/
│       ├── FeatureSubscription/
│       ├── InfraSTT/
│       ├── InfraLLM/
│       ├── InfraStorage/
│       ├── InfraNetwork/
│       ├── SharedUI/
│       └── SharedUtil/
└── .swiftlint.yml
```

- `platforms: [.iOS(.v17)]` を設定
- Swift 5.9 以上

### 2. Package.swift SPM構成 :large_blue_circle:

統合仕様書セクション9の命名規則、および01-Archセクション3.3に準拠したPackage.swift。

```swift
let package = Package(
    name: "VoiceMemoModules",
    platforms: [.iOS(.v17)],
    products: [
        .library(name: "FeatureRecording", targets: ["FeatureRecording"]),
        .library(name: "FeatureMemo", targets: ["FeatureMemo"]),
        .library(name: "FeatureAI", targets: ["FeatureAI"]),
        .library(name: "FeatureSearch", targets: ["FeatureSearch"]),
        .library(name: "FeatureSettings", targets: ["FeatureSettings"]),
        .library(name: "FeatureSubscription", targets: ["FeatureSubscription"]),
        .library(name: "Domain", targets: ["Domain"]),
        .library(name: "Data", targets: ["Data"]),
        .library(name: "InfraSTT", targets: ["InfraSTT"]),
        .library(name: "InfraLLM", targets: ["InfraLLM"]),
        .library(name: "InfraStorage", targets: ["InfraStorage"]),
        .library(name: "InfraNetwork", targets: ["InfraNetwork"]),
        .library(name: "SharedUI", targets: ["SharedUI"]),
        .library(name: "SharedUtil", targets: ["SharedUtil"]),
    ],
    dependencies: [
        .package(url: "https://github.com/pointfreeco/swift-composable-architecture", from: "1.17.0"),
        .package(url: "https://github.com/argmaxinc/WhisperKit", from: "0.9.0"),
    ],
    targets: [
        .target(name: "Domain"),
        .target(name: "SharedUtil"),
        .target(name: "SharedUI"),
        .target(name: "InfraSTT", dependencies: ["Domain"]),
        .target(name: "InfraLLM", dependencies: ["Domain"]),
        .target(name: "InfraStorage", dependencies: ["SharedUtil"]),
        .target(name: "InfraNetwork", dependencies: ["SharedUtil"]),
        .target(name: "Data", dependencies: ["Domain", "InfraStorage", "InfraNetwork"]),
        // Feature -> Domain + SharedUI のみ（Infra直接依存禁止: 統合仕様書H2準拠）
        .target(name: "FeatureRecording", dependencies: [
            "Domain", "SharedUI",
            .product(name: "ComposableArchitecture", package: "swift-composable-architecture"),
        ]),
        .target(name: "FeatureMemo", dependencies: [
            "Domain", "SharedUI",
            .product(name: "ComposableArchitecture", package: "swift-composable-architecture"),
        ]),
        .target(name: "FeatureAI", dependencies: [
            "Domain", "SharedUI",
            .product(name: "ComposableArchitecture", package: "swift-composable-architecture"),
        ]),
        .target(name: "FeatureSearch", dependencies: [
            "Domain", "SharedUI",
            .product(name: "ComposableArchitecture", package: "swift-composable-architecture"),
        ]),
        .target(name: "FeatureSettings", dependencies: [
            "Domain", "SharedUI",
            .product(name: "ComposableArchitecture", package: "swift-composable-architecture"),
        ]),
        .target(name: "FeatureSubscription", dependencies: [
            "Domain", "SharedUI",
            .product(name: "ComposableArchitecture", package: "swift-composable-architecture"),
        ]),
    ]
)
```

**重要**: Feature -> Infra 直接依存を排除。Feature モジュールは Domain 層のプロトコル経由でのみ Infra にアクセスし、実体の注入は VoiceMemoApp（DIコンテナ）で `@Dependency` を通じて行う。

### 3. TCA最小動作確認 :large_blue_circle:

```swift
// VoiceMemoApp/VoiceMemoApp.swift
@main
struct VoiceMemoApp: App {
    var body: some Scene {
        WindowGroup {
            AppView(
                store: Store(initialState: AppReducer.State()) {
                    AppReducer()
                }
            )
        }
    }
}

// AppReducer（最小実装）
@Reducer
struct AppReducer {
    @ObservableState
    struct State {
        var selectedTab: Tab = .home
        enum Tab: Hashable { case home, search, stats, settings }
    }
    enum Action { case tabSelected(State.Tab) }
    var body: some ReducerOf<Self> {
        Reduce { state, action in
            switch action {
            case let .tabSelected(tab):
                state.selectedTab = tab
                return .none
            }
        }
    }
}
```

### 4. SwiftLint設定 :large_blue_circle:

`.swiftlint.yml` に以下のルールを設定:
- `line_length: 120`
- `type_body_length: 300`
- `function_body_length: 50`
- `disabled_rules: [trailing_whitespace]`
- `included: ["VoiceMemoApp", "VoiceMemoModules/Sources"]`

### 5. .gitignore設定 :large_blue_circle:

Xcode / Swift / SPM / CocoaPods / macOS関連の除外設定。
- `*.xcuserdata`
- `.build/`
- `DerivedData/`
- `.swiftpm/`
- `Pods/`
- `.DS_Store`

## テスト要件
DIRECTタスクのため、TDDサイクルは適用しない。以下を手動検証する。

### 検証項目
- `xcodebuild build` がエラーなく完了する
- 14モジュール全てのターゲットがビルドに含まれる
- TCA StoreでAppReducerが動作する（タブ切替が反映される）
- SwiftLintがビルド時に実行される

## 実装手順
1. **direct-setup**: Xcodeプロジェクト作成 -> SPM Package.swift定義 -> 各モジュールのディレクトリ・ソースファイル作成 -> TCA導入 -> AppReducer最小実装 -> SwiftLint設定 -> .gitignore作成
2. **direct-verify**: `xcodebuild build` 成功確認 -> 全モジュールターゲット存在確認 -> TCA Store動作確認

## 信頼性レベルサマリー
- :large_blue_circle:: 5件（全て設計書準拠）
- :yellow_circle:: 0件
- :red_circle:: 0件
