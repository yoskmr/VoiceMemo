# TASK-0018: カスタム辞書（STT精度向上）

> **タスクID**: TASK-0018
> **フェーズ**: Phase 2 - メモ管理 + 検索
> **タイプ**: TDD
> **見積工数**: 4h
> **依存タスク**: TASK-0007
> **関連要件**: REQ-025
> **ステータス**: TODO
> **担当モジュール**: FeatureSettings, Domain, InfraSTT

---

## 1. 概要

よく使う単語の登録・管理UIを実装し、SFSpeechRecognizerのcontextualStrings（コンテキストヒント）と連携させる。カスタム辞書はSwiftDataで永続化し、STTエンジンの認識精度を向上させる。設定画面（設計書セクション6.5）の「カスタム辞書」から遷移する。

---

## 2. 完了条件（Definition of Done）

- [ ] CustomDictionaryReducer のユニットテスト（TestStore）が全てパス
- [ ] 単語の追加（読み + 表記）ができる
- [ ] 単語の削除ができる
- [ ] 単語の一覧が表示される
- [ ] 登録済み単語がSFSpeechRecognizer.contextualStringsに反映される
- [ ] カスタム辞書がSwiftData（UserSettings.customDictionary）で永続化される
- [ ] 空の単語や重複単語の登録がバリデーションで防止される
- [ ] VoiceOverアクセシビリティ対応が完了している

---

## 3. 技術仕様

### 3.1 TCA State / Action / Reducer

```swift
// FeatureSettings/CustomDictionary/CustomDictionaryReducer.swift
import ComposableArchitecture
import Foundation

@Reducer
struct CustomDictionaryReducer {
    @ObservableState
    struct State: Equatable {
        var entries: IdentifiedArrayOf<DictionaryEntry> = []
        var newReading: String = ""      // 読み（ひらがな/カタカナ）
        var newDisplay: String = ""      // 表記（漢字/英語等）
        var isAdding: Bool = false
        var validationError: String?
        var errorMessage: String?

        struct DictionaryEntry: Equatable, Identifiable {
            let id: UUID
            var reading: String   // 読み
            var display: String   // 表記
        }
    }

    enum Action: BindableAction {
        case binding(BindingAction<State>)
        case onAppear
        case entriesLoaded(Result<[State.DictionaryEntry], Error>)
        case newReadingChanged(String)
        case newDisplayChanged(String)
        case addButtonTapped
        case addCompleted(Result<State.DictionaryEntry, Error>)
        case deleteEntry(id: UUID)
        case deleteCompleted(Result<UUID, Error>)
        case dismissError
    }

    @Dependency(\.customDictionaryClient) var dictionaryClient

    var body: some ReducerOf<Self> {
        BindingReducer()

        Reduce { state, action in
            switch action {
            case .onAppear:
                return .run { send in
                    let result = await Result {
                        try await dictionaryClient.loadEntries()
                    }
                    await send(.entriesLoaded(result))
                }

            case let .entriesLoaded(.success(entries)):
                state.entries = IdentifiedArrayOf(uniqueElements: entries)
                return .none

            case let .entriesLoaded(.failure(error)):
                state.errorMessage = error.localizedDescription
                return .none

            case let .newReadingChanged(reading):
                state.newReading = reading
                state.validationError = nil
                return .none

            case let .newDisplayChanged(display):
                state.newDisplay = display
                state.validationError = nil
                return .none

            case .addButtonTapped:
                // バリデーション
                let reading = state.newReading.trimmingCharacters(in: .whitespaces)
                let display = state.newDisplay.trimmingCharacters(in: .whitespaces)

                if reading.isEmpty || display.isEmpty {
                    state.validationError = "読みと表記の両方を入力してください"
                    return .none
                }

                if state.entries.contains(where: { $0.reading == reading && $0.display == display }) {
                    state.validationError = "この単語は既に登録されています"
                    return .none
                }

                state.isAdding = true
                state.validationError = nil
                let entry = State.DictionaryEntry(
                    id: UUID(),
                    reading: reading,
                    display: display
                )

                return .run { send in
                    let result = await Result {
                        try await dictionaryClient.addEntry(entry)
                        return entry
                    }
                    await send(.addCompleted(result))
                }

            case let .addCompleted(.success(entry)):
                state.isAdding = false
                state.entries.append(entry)
                state.newReading = ""
                state.newDisplay = ""
                return .none

            case let .addCompleted(.failure(error)):
                state.isAdding = false
                state.errorMessage = error.localizedDescription
                return .none

            case let .deleteEntry(id):
                return .run { send in
                    let result = await Result {
                        try await dictionaryClient.deleteEntry(id: id)
                        return id
                    }
                    await send(.deleteCompleted(result))
                }

            case let .deleteCompleted(.success(id)):
                state.entries.remove(id: id)
                return .none

            case let .deleteCompleted(.failure(error)):
                state.errorMessage = error.localizedDescription
                return .none

            case .dismissError:
                state.errorMessage = nil
                state.validationError = nil
                return .none

            case .binding:
                return .none
            }
        }
    }
}
```

### 3.2 CustomDictionaryClient（Dependency）

```swift
// Domain/Protocols/CustomDictionaryClient.swift
import Dependencies

struct CustomDictionaryClient {
    var loadEntries: @Sendable () async throws -> [CustomDictionaryReducer.State.DictionaryEntry]
    var addEntry: @Sendable (CustomDictionaryReducer.State.DictionaryEntry) async throws -> Void
    var deleteEntry: @Sendable (UUID) async throws -> Void
    var getContextualStrings: @Sendable () async throws -> [String]
}

extension CustomDictionaryClient: DependencyKey {
    static let liveValue: Self = {
        Self(
            loadEntries: {
                // SwiftData UserSettings.customDictionary から読み込み
                // {"読み": "表記"} → DictionaryEntry 配列に変換
                // ...
            },
            addEntry: { entry in
                // SwiftData UserSettings.customDictionary に追加
                // ...
            },
            deleteEntry: { id in
                // SwiftData UserSettings.customDictionary から削除
                // ...
            },
            getContextualStrings: {
                // STTエンジンに渡す contextualStrings を生成
                // 表記のリストを返す（SFSpeechRecognizer用）
                // ...
            }
        )
    }()

    static let testValue: Self = Self(
        loadEntries: { [] },
        addEntry: { _ in },
        deleteEntry: { _ in },
        getContextualStrings: { [] }
    )
}
```

### 3.3 SFSpeechRecognizer との連携

```swift
// InfraSTT/SpeechAnalyzer/SpeechAnalyzerEngine.swift
// 既存のSTTエンジンにカスタム辞書連携を追加

extension SpeechAnalyzerEngine {
    /// カスタム辞書の contextualStrings をリクエストに設定
    /// REQ-025: SFSpeechRecognizer のコンテキストヒント
    func applyCustomDictionary(
        to request: SFSpeechAudioBufferRecognitionRequest,
        entries: [String]
    ) {
        // contextualStrings: 音声認識の精度を向上させるヒントワード
        // SFSpeechRecognizer は認識時にこれらの単語を優先的に候補として提案する
        request.contextualStrings = entries
    }
}

// RecordingReducer 内での使用例
// let contextualStrings = try await customDictionaryClient.getContextualStrings()
// sttEngine.applyCustomDictionary(to: request, entries: contextualStrings)
```

### 3.4 SwiftData永続化

```swift
// UserSettings.customDictionary の構造
// 設計書 01-system-architecture.md セクション5.2 準拠
//
// UserSettings {
//     var customDictionary: [String: String]  // {"読み": "表記"} 形式
// }
//
// DictionaryEntry ↔ customDictionary の変換:
//   entry.reading → key
//   entry.display → value
//   entry.id → 読み+表記のハッシュから生成（or 別途管理）
```

### 3.5 CustomDictionaryView（SwiftUI）

```swift
// FeatureSettings/CustomDictionary/CustomDictionaryView.swift
// 設計書 04-ui-design-system.md セクション6.5 設定画面 → カスタム辞書

struct CustomDictionaryView: View {
    @Bindable var store: StoreOf<CustomDictionaryReducer>

    var body: some View {
        List {
            // 新規追加セクション
            Section {
                VStack(spacing: 12) {
                    HStack(spacing: 12) {
                        VStack(alignment: .leading, spacing: 4) {
                            Text("読み")
                                .font(.vmCaption1)
                                .foregroundColor(.vmTextSecondary)
                            TextField("よみがな", text: $store.newReading.sending(\.newReadingChanged))
                                .font(.vmBody())
                                .textFieldStyle(.roundedBorder)
                        }

                        VStack(alignment: .leading, spacing: 4) {
                            Text("表記")
                                .font(.vmCaption1)
                                .foregroundColor(.vmTextSecondary)
                            TextField("漢字・英語等", text: $store.newDisplay.sending(\.newDisplayChanged))
                                .font(.vmBody())
                                .textFieldStyle(.roundedBorder)
                        }
                    }

                    if let error = store.validationError {
                        Text(error)
                            .font(.vmCaption1)
                            .foregroundColor(.vmError)
                    }

                    Button {
                        store.send(.addButtonTapped)
                    } label: {
                        HStack {
                            if store.isAdding {
                                ProgressView()
                                    .scaleEffect(0.8)
                            }
                            Text("追加")
                                .font(.vmHeadline)
                        }
                        .frame(maxWidth: .infinity)
                        .padding(.vertical, 10)
                        .background(Color.vmPrimary)
                        .foregroundColor(.white)
                        .cornerRadius(12)
                    }
                    .disabled(store.isAdding)
                }
            } header: {
                Text("新しい単語を登録")
            }

            // 登録済み単語一覧
            Section {
                if store.entries.isEmpty {
                    HStack {
                        Spacer()
                        VStack(spacing: 8) {
                            Image(systemName: "character.book.closed")
                                .font(.system(size: 32))
                                .foregroundColor(.vmTextTertiary)
                            Text("登録された単語はありません")
                                .font(.vmCallout)
                                .foregroundColor(.vmTextTertiary)
                        }
                        Spacer()
                    }
                    .padding(.vertical, 20)
                } else {
                    ForEach(store.entries) { entry in
                        HStack {
                            VStack(alignment: .leading, spacing: 2) {
                                Text(entry.display)
                                    .font(.vmHeadline)
                                    .foregroundColor(.vmTextPrimary)
                                Text(entry.reading)
                                    .font(.vmCaption1)
                                    .foregroundColor(.vmTextSecondary)
                            }
                            Spacer()
                        }
                        .accessibilityElement(children: .combine)
                        .accessibilityLabel("\(entry.display)、読み: \(entry.reading)")
                    }
                    .onDelete { indexSet in
                        for index in indexSet {
                            let entry = store.entries[index]
                            store.send(.deleteEntry(id: entry.id))
                        }
                    }
                }
            } header: {
                Text("登録済み単語（\(store.entries.count)件）")
            } footer: {
                Text("登録した単語は音声認識時に優先的に認識されます。専門用語や固有名詞の登録が効果的です。")
                    .font(.vmCaption2)
            }
        }
        .listStyle(.insetGrouped)
        .navigationTitle("カスタム辞書")
        .navigationBarTitleDisplayMode(.inline)
        .alert("エラー", isPresented: .init(
            get: { store.errorMessage != nil },
            set: { if !$0 { store.send(.dismissError) } }
        )) {
            Button("OK") { store.send(.dismissError) }
        } message: {
            Text(store.errorMessage ?? "")
        }
        .onAppear { store.send(.onAppear) }
    }
}
```

---

## 4. TDDテスト計画

### 4.1 Red-Green-Refactor サイクル

| # | テストケース | 検証内容 |
|:--|:------------|:---------|
| 1 | `test_onAppear_辞書エントリのロード` | onAppear→entriesLoaded→entries配列が設定される |
| 2 | `test_addButtonTapped_正常追加` | 読み+表記入力→addButtonTapped→entries に追加 |
| 3 | `test_addButtonTapped_追加後に入力欄クリア` | addCompleted→newReading="" / newDisplay="" |
| 4 | `test_addButtonTapped_空入力バリデーション` | 読み空→validationError設定、追加されない |
| 5 | `test_addButtonTapped_表記空バリデーション` | 表記空→validationError設定 |
| 6 | `test_addButtonTapped_重複バリデーション` | 同じ読み+表記→validationError="既に登録されています" |
| 7 | `test_deleteEntry_エントリ削除` | deleteEntry(id:)→entries から削除 |
| 8 | `test_deleteEntry_failure_エラー表示` | 削除失敗→errorMessage設定 |
| 9 | `test_addCompleted_failure_エラー表示` | 追加失敗→errorMessage設定、isAdding=false |
| 10 | `test_getContextualStrings_表記リスト返却` | entries の display 値の配列が返される |
| 11 | `test_newReadingChanged_バリデーションエラークリア` | 入力変更→validationError=nil |

### 4.2 TestStore テスト例

```swift
@MainActor
func test_addButtonTapped_正常追加() async {
    let store = TestStore(
        initialState: CustomDictionaryReducer.State()
    ) {
        CustomDictionaryReducer()
    } withDependencies: {
        $0.customDictionaryClient.addEntry = { _ in }
    }

    await store.send(.newReadingChanged("こんぽーざぶる")) {
        $0.newReading = "こんぽーざぶる"
    }

    await store.send(.newDisplayChanged("Composable")) {
        $0.newDisplay = "Composable"
    }

    await store.send(.addButtonTapped) {
        $0.isAdding = true
        $0.validationError = nil
    }

    await store.receive(.addCompleted(.success(/* entry */))) {
        $0.isAdding = false
        $0.entries = [/* entry */]
        $0.newReading = ""
        $0.newDisplay = ""
    }
}

@MainActor
func test_addButtonTapped_重複バリデーション() async {
    let existingEntry = CustomDictionaryReducer.State.DictionaryEntry(
        id: UUID(), reading: "てすと", display: "テスト"
    )

    let store = TestStore(
        initialState: CustomDictionaryReducer.State(
            entries: [existingEntry],
            newReading: "てすと",
            newDisplay: "テスト"
        )
    ) {
        CustomDictionaryReducer()
    }

    await store.send(.addButtonTapped) {
        $0.validationError = "この単語は既に登録されています"
    }
}
```

---

## 5. 依存関係

### 5.1 前提タスク

| タスクID | 内容 | 必要な成果物 |
|:---------|:-----|:------------|
| TASK-0007 | STTエンジン基盤 | SFSpeechRecognizer / SpeechAnalyzerEngine の基本実装 |

### 5.2 後続タスク

なし（独立した辞書管理機能。ただし録音時にcontextualStringsとして参照される）

### 5.3 @Dependency

| 依存名 | プロトコル | 提供モジュール |
|:-------|:----------|:--------------|
| customDictionaryClient | CustomDictionaryClient | Domain / Data |

---

## 6. 実装手順

1. **CustomDictionaryClient の依存定義**（protocol + testValue）
2. **CustomDictionaryReducer の State / Action 定義**
3. **TestStoreテスト: onAppear / addButtonTapped** を記述（Red）
4. **Reducer body 実装**: onAppear, addButtonTapped, addCompleted（Green）
5. **バリデーションのテスト・実装**（空入力、重複）
6. **deleteEntry のテスト・実装**
7. **CustomDictionaryClient.liveValue の実装**（SwiftData連携）
8. **SFSpeechRecognizer.contextualStrings 連携の実装**
9. **CustomDictionaryView の SwiftUI 実装**
10. **アクセシビリティ確認**

---

## 7. 設計書参照

| 設計書 | セクション | 内容 |
|:-------|:----------|:-----|
| 01-system-architecture.md | 5.2 SwiftData @Model | UserSettings.customDictionary の定義（[String: String]） |
| 01-system-architecture.md | 3.2 モジュール一覧 | FeatureSettings の責務（辞書管理） |
| 04-ui-design-system.md | 5.2 ナビゲーションフロー | 設定→カスタム辞書への遷移 |
| 04-ui-design-system.md | 6.5 設定画面 | カスタム辞書の配置位置 |
