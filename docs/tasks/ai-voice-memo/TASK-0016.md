# TASK-0016: 検索UI画面

> **タスクID**: TASK-0016
> **フェーズ**: Phase 2 - メモ管理 + 検索
> **タイプ**: TDD
> **見積工数**: 8h
> **依存タスク**: TASK-0015, TASK-0011
> **関連要件**: REQ-006
> **ステータス**: TODO
> **担当モジュール**: FeatureSearch, SharedUI

---

## 1. 概要

全文検索UIを実装する。検索バーからのリアルタイム検索（デバウンス300ms）、検索結果リスト（キーワードハイライト付き）、日付フィルター、タグフィルターを含む。FTS5検索エンジン（TASK-0015）と連携し、検索結果のスニペット表示を行う。

---

## 2. 完了条件（Definition of Done）

- [ ] SearchReducer のユニットテスト（TestStore）が全てパス
- [ ] 検索バーにテキスト入力後300msデバウンスで自動検索が実行される
- [ ] 検索結果がリスト表示される（メモタイトル、日時、感情バッジ、録音時間）
- [ ] 検索キーワードがスニペット内でハイライト表示される
- [ ] 検索結果件数が表示される
- [ ] 日付フィルター（今日、過去1週間、過去1ヶ月、全期間）が動作する
- [ ] タグフィルター（選択/解除）が動作する
- [ ] 検索結果タップでメモ詳細画面に遷移する
- [ ] 空の検索結果時に適切なメッセージが表示される
- [ ] VoiceOverアクセシビリティ対応が完了している
- [ ] ダークモード対応が確認済み

---

## 3. 技術仕様

### 3.1 TCA State / Action / Reducer

```swift
// FeatureSearch/SearchReducer.swift
import ComposableArchitecture
import Foundation

@Reducer
struct SearchReducer {
    @ObservableState
    struct State: Equatable {
        var searchText: String = ""
        var results: [SearchResultItem] = []
        var resultCount: Int = 0
        var isSearching: Bool = false

        // フィルター
        var showFilters: Bool = false
        var selectedDateFilter: DateFilter = .all
        var selectedTags: Set<String> = []
        var availableTags: [String] = []  // タグ一覧

        // UI
        var errorMessage: String?
        var isInitialState: Bool = true  // 検索未実行状態

        enum DateFilter: String, CaseIterable, Equatable {
            case today = "今日"
            case week = "過去1週間"
            case month = "過去1ヶ月"
            case all = "全期間"

            var startDate: Date? {
                let calendar = Calendar.current
                let now = Date()
                switch self {
                case .today: return calendar.startOfDay(for: now)
                case .week: return calendar.date(byAdding: .day, value: -7, to: now)
                case .month: return calendar.date(byAdding: .month, value: -1, to: now)
                case .all: return nil
                }
            }
        }
    }

    struct SearchResultItem: Equatable, Identifiable {
        let id: UUID  // memoID
        var title: String
        var snippet: String          // ハイライトマーク付きスニペット
        var createdAt: Date
        var emotion: EmotionCategory?
        var durationSeconds: Double
        var tags: [String]
    }

    enum Action: BindableAction {
        case binding(BindingAction<State>)
        case onAppear
        case searchTextChanged(String)
        case performSearch
        case searchCompleted(Result<[SearchResultItem], Error>)
        case resultTapped(id: UUID)

        // フィルター
        case toggleFilters
        case dateFilterChanged(State.DateFilter)
        case tagFilterToggled(String)
        case clearFilters

        // タグ一覧
        case availableTagsLoaded([String])
    }

    @Dependency(\.fts5IndexManager) var fts5IndexManager
    @Dependency(\.memoRepository) var memoRepository
    @Dependency(\.continuousClock) var clock

    private enum SearchDebounceID { case debounce }

    var body: some ReducerOf<Self> {
        BindingReducer()

        Reduce { state, action in
            switch action {
            case .onAppear:
                return .run { send in
                    let tags = try await memoRepository.fetchAllTags()
                    await send(.availableTagsLoaded(tags))
                }

            case let .availableTagsLoaded(tags):
                state.availableTags = tags
                return .none

            case let .searchTextChanged(text):
                state.searchText = text
                state.isInitialState = false

                guard !text.trimmingCharacters(in: .whitespaces).isEmpty else {
                    state.results = []
                    state.resultCount = 0
                    return .cancel(id: SearchDebounceID.debounce)
                }

                // 300msデバウンス
                return .run { send in
                    try await clock.sleep(for: .milliseconds(300))
                    await send(.performSearch)
                }
                .cancellable(id: SearchDebounceID.debounce, cancelInFlight: true)

            case .performSearch:
                let query = state.searchText
                let dateFilter = state.selectedDateFilter
                let tagFilter = state.selectedTags

                guard !query.trimmingCharacters(in: .whitespaces).isEmpty else {
                    state.results = []
                    state.resultCount = 0
                    return .none
                }

                state.isSearching = true
                return .run { send in
                    let result = await Result {
                        // FTS5検索実行
                        let ftsResults = try fts5IndexManager.searchWithSnippets(
                            query: query,
                            snippetColumn: 2,
                            maxTokens: 32
                        )

                        // メモ詳細情報を取得してフィルタリング
                        var items: [SearchResultItem] = []
                        for ftsResult in ftsResults {
                            guard let memo = try await memoRepository.fetchMemoForSearch(
                                id: ftsResult.memoID
                            ) else { continue }

                            // 日付フィルター
                            if let startDate = dateFilter.startDate,
                               memo.createdAt < startDate {
                                continue
                            }

                            // タグフィルター
                            if !tagFilter.isEmpty {
                                let memoTags = Set(memo.tags)
                                guard !memoTags.isDisjoint(with: tagFilter) else { continue }
                            }

                            items.append(SearchResultItem(
                                id: UUID(uuidString: ftsResult.memoID) ?? UUID(),
                                title: memo.title,
                                snippet: ftsResult.snippet,
                                createdAt: memo.createdAt,
                                emotion: memo.emotion,
                                durationSeconds: memo.durationSeconds,
                                tags: memo.tags
                            ))
                        }
                        return items
                    }
                    await send(.searchCompleted(result))
                }

            case let .searchCompleted(.success(items)):
                state.isSearching = false
                state.results = items
                state.resultCount = items.count
                return .none

            case let .searchCompleted(.failure(error)):
                state.isSearching = false
                state.errorMessage = error.localizedDescription
                return .none

            case .toggleFilters:
                state.showFilters.toggle()
                return .none

            case let .dateFilterChanged(filter):
                state.selectedDateFilter = filter
                return .send(.performSearch)

            case let .tagFilterToggled(tag):
                if state.selectedTags.contains(tag) {
                    state.selectedTags.remove(tag)
                } else {
                    state.selectedTags.insert(tag)
                }
                return .send(.performSearch)

            case .clearFilters:
                state.selectedDateFilter = .all
                state.selectedTags = []
                return .send(.performSearch)

            case .resultTapped:
                // 親Reducerで画面遷移を処理
                return .none

            case .binding:
                return .none
            }
        }
    }
}
```

### 3.2 SearchView（SwiftUI）

```swift
// FeatureSearch/SearchView.swift
// 設計書 04-ui-design-system.md セクション6.4 準拠

struct SearchView: View {
    @Bindable var store: StoreOf<SearchReducer>

    var body: some View {
        VStack(spacing: 0) {
            // 検索バー（設計書 セクション4.6）
            VMSearchBar(
                text: $store.searchText.sending(\.searchTextChanged),
                showFilters: $store.showFilters.sending(\.toggleFilters),
                isPro: true  // TODO: サブスクリプション状態から取得
            )
            .padding(.horizontal, 16)
            .padding(.vertical, 8)

            // フィルターパネル（展開時のみ）
            if store.showFilters {
                FilterPanel(store: store)
                    .transition(.move(edge: .top).combined(with: .opacity))
                    .animation(.vmNavigationPush, value: store.showFilters)
            }

            // 検索結果
            if store.isInitialState {
                // 初期状態: 検索ヒント表示
                SearchHintView()
            } else if store.isSearching {
                ProgressView()
                    .padding()
            } else if store.results.isEmpty && !store.searchText.isEmpty {
                // 検索結果なし
                EmptySearchResultView(query: store.searchText)
            } else {
                // 検索結果カウント
                if store.resultCount > 0 {
                    HStack {
                        Text("\(store.resultCount)件の結果")
                            .font(.vmSubheadline)
                            .foregroundColor(.vmTextSecondary)
                        Spacer()
                    }
                    .padding(.horizontal, 16)
                    .padding(.vertical, 4)
                }

                // 検索結果リスト
                ScrollView {
                    LazyVStack(spacing: 8) {
                        ForEach(store.results) { result in
                            SearchResultCard(result: result)
                                .onTapGesture {
                                    store.send(.resultTapped(id: result.id))
                                }
                                .padding(.horizontal, 16)
                        }
                    }
                    .padding(.vertical, 8)
                }
            }
        }
        .background(Color.vmBackground)
        .onAppear { store.send(.onAppear) }
    }
}
```

### 3.3 サブコンポーネント

```swift
// フィルターパネル
struct FilterPanel: View {
    @Bindable var store: StoreOf<SearchReducer>

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            // 日付フィルター
            VStack(alignment: .leading, spacing: 6) {
                Text("期間")
                    .font(.vmCaption1)
                    .foregroundColor(.vmTextSecondary)
                HStack(spacing: 8) {
                    ForEach(SearchReducer.State.DateFilter.allCases, id: \.self) { filter in
                        FilterChip(
                            label: filter.rawValue,
                            isSelected: store.selectedDateFilter == filter
                        ) {
                            store.send(.dateFilterChanged(filter))
                        }
                    }
                }
            }

            // タグフィルター
            if !store.availableTags.isEmpty {
                VStack(alignment: .leading, spacing: 6) {
                    Text("タグ")
                        .font(.vmCaption1)
                        .foregroundColor(.vmTextSecondary)

                    FlowLayout(spacing: 8) {
                        ForEach(store.availableTags, id: \.self) { tag in
                            FilterChip(
                                label: tag,
                                isSelected: store.selectedTags.contains(tag)
                            ) {
                                store.send(.tagFilterToggled(tag))
                            }
                        }
                    }
                }
            }

            // クリアボタン
            if store.selectedDateFilter != .all || !store.selectedTags.isEmpty {
                Button {
                    store.send(.clearFilters)
                } label: {
                    Text("フィルターをクリア")
                        .font(.vmCaption1)
                        .foregroundColor(.vmPrimary)
                }
            }
        }
        .padding(16)
        .background(Color.vmSurfaceVariant)
        .cornerRadius(16)
        .padding(.horizontal, 16)
    }
}

// フィルターチップ
struct FilterChip: View {
    let label: String
    let isSelected: Bool
    let onTap: () -> Void

    var body: some View {
        Text(label)
            .font(.vmCaption1)
            .padding(.horizontal, 12)
            .padding(.vertical, 6)
            .background(isSelected ? Color.vmPrimary : Color.vmSurfaceVariant)
            .foregroundColor(isSelected ? .white : .vmTextSecondary)
            .cornerRadius(12)
            .onTapGesture(perform: onTap)
    }
}

// 検索結果カード（ハイライト付き）
struct SearchResultCard: View {
    let result: SearchReducer.SearchResultItem

    var body: some View {
        VStack(alignment: .leading, spacing: 6) {
            // タイトル + 日時
            HStack {
                Text(result.title)
                    .font(.vmHeadline)
                    .foregroundColor(.vmTextPrimary)
                    .lineLimit(1)
                Spacer()
                Text(formattedDate(result.createdAt))
                    .font(.vmCaption1)
                    .foregroundColor(.vmTextTertiary)
            }

            // スニペット（ハイライト付き）
            HighlightedSnippetText(snippet: result.snippet)
                .font(.vmCallout)
                .lineLimit(2)

            // メタ情報
            HStack(spacing: 8) {
                if let emotion = result.emotion {
                    EmotionBadge(emotion: emotion)
                }
                Label(formattedDuration(result.durationSeconds), systemImage: "mic.fill")
                    .font(.vmCaption1)
                    .foregroundColor(.vmTextTertiary)
            }
        }
        .padding(12)
        .background(Color.vmSurface)
        .cornerRadius(12)
        .accessibilityElement(children: .combine)
        .accessibilityLabel("\(result.title)、\(formattedDate(result.createdAt))")
        .accessibilityHint("ダブルタップで詳細を表示します")
    }
}

// ハイライト付きスニペットテキスト
// <mark>キーワード</mark> を AttributedString でハイライト表示
struct HighlightedSnippetText: View {
    let snippet: String

    var body: some View {
        Text(attributedSnippet)
    }

    private var attributedSnippet: AttributedString {
        var result = AttributedString()
        let parts = snippet.components(separatedBy: "<mark>")

        for (index, part) in parts.enumerated() {
            if index == 0 {
                var attr = AttributedString(part)
                attr.foregroundColor = .vmTextSecondary
                result.append(attr)
            } else {
                let subParts = part.components(separatedBy: "</mark>")
                if subParts.count == 2 {
                    // ハイライト部分
                    var highlighted = AttributedString(subParts[0])
                    highlighted.foregroundColor = .vmTextPrimary
                    highlighted.backgroundColor = .vmAccentLight
                    result.append(highlighted)

                    // 通常部分
                    var normal = AttributedString(subParts[1])
                    normal.foregroundColor = .vmTextSecondary
                    result.append(normal)
                }
            }
        }
        return result
    }
}

// 検索ヒント（初期状態）
struct SearchHintView: View {
    var body: some View {
        VStack(spacing: 12) {
            Spacer()
            Image(systemName: "magnifyingglass")
                .font(.system(size: 40))
                .foregroundColor(.vmTextTertiary)
            Text("メモを検索")
                .font(.vmTitle3)
                .foregroundColor(.vmTextSecondary)
            Text("キーワードを入力して\nメモを検索できます")
                .font(.vmCallout)
                .foregroundColor(.vmTextTertiary)
                .multilineTextAlignment(.center)
            Spacer()
        }
    }
}

// 検索結果なし
struct EmptySearchResultView: View {
    let query: String

    var body: some View {
        VStack(spacing: 12) {
            Spacer()
            Image(systemName: "doc.text.magnifyingglass")
                .font(.system(size: 40))
                .foregroundColor(.vmTextTertiary)
            Text("「\(query)」に一致するメモはありません")
                .font(.vmCallout)
                .foregroundColor(.vmTextSecondary)
                .multilineTextAlignment(.center)
            Spacer()
        }
    }
}
```

---

## 4. TDDテスト計画

### 4.1 Red-Green-Refactor サイクル

| # | テストケース | 検証内容 |
|:--|:------------|:---------|
| 1 | `test_searchTextChanged_300msデバウンス` | テキスト入力→300ms経過→performSearchが呼ばれる |
| 2 | `test_searchTextChanged_連続入力でデバウンスリセット` | 入力→200ms→再入力→300ms→検索は1回のみ |
| 3 | `test_searchTextChanged_空文字で結果クリア` | 空文字入力→results=[], resultCount=0 |
| 4 | `test_performSearch_検索結果が返される` | performSearch→searchCompleted→resultsに結果が設定 |
| 5 | `test_performSearch_結果件数表示` | 3件の結果→resultCount=3 |
| 6 | `test_performSearch_failure_エラー表示` | 検索エラー→errorMessageが設定 |
| 7 | `test_dateFilterChanged_フィルター適用後に再検索` | dateFilterChanged(.week)→performSearchが呼ばれる |
| 8 | `test_tagFilterToggled_タグ選択/解除` | タグ選択→selectedTagsに追加、再度→削除 |
| 9 | `test_clearFilters_全フィルターリセット` | clearFilters→selectedDateFilter=.all, selectedTags=[] |
| 10 | `test_resultTapped_アクション伝播` | resultTapped(id:)→親Reducerに伝播 |
| 11 | `test_onAppear_タグ一覧ロード` | onAppear→availableTagsLoadedでタグ一覧設定 |
| 12 | `test_dateFilter_今日のみ` | dateFilter=.today→今日のメモのみ結果に含まれる |
| 13 | `test_tagFilter_選択タグのメモのみ` | tagFilter={"アイデア"}→該当タグのメモのみ |

### 4.2 TestStore テスト例

```swift
@MainActor
func test_searchTextChanged_300msデバウンス() async {
    let testClock = TestClock()

    let store = TestStore(
        initialState: SearchReducer.State()
    ) {
        SearchReducer()
    } withDependencies: {
        $0.continuousClock = testClock
        $0.fts5IndexManager = MockFTS5IndexManager(results: [
            .init(memoID: "memo-1", snippet: "テスト<mark>アイデア</mark>メモ", rank: -1.0)
        ])
        $0.memoRepository.fetchMemoForSearch = { _ in
            .init(title: "テスト", createdAt: Date(), emotion: .joy, durationSeconds: 120, tags: ["アイデア"])
        }
    }

    await store.send(.searchTextChanged("アイデア")) {
        $0.searchText = "アイデア"
        $0.isInitialState = false
    }

    // 300ms前に検索が実行されないことを確認
    await testClock.advance(by: .milliseconds(200))

    // 300ms経過で検索実行
    await testClock.advance(by: .milliseconds(100))

    await store.receive(.performSearch) {
        $0.isSearching = true
    }

    await store.receive(.searchCompleted(.success([/* expected results */]))) {
        $0.isSearching = false
        $0.resultCount = 1
        // ... results の検証
    }
}
```

---

## 5. 依存関係

### 5.1 前提タスク

| タスクID | 内容 | 必要な成果物 |
|:---------|:-----|:------------|
| TASK-0015 | SQLite FTS5全文検索エンジン | FTS5IndexManagerProtocol.search() / searchWithSnippets() |
| TASK-0011 | メモ一覧画面 | EmotionBadge, TagChip コンポーネント（再利用） |

### 5.2 後続タスク

なし（独立した検索機能）

### 5.3 @Dependency

| 依存名 | プロトコル | 提供モジュール |
|:-------|:----------|:--------------|
| fts5IndexManager | FTS5IndexManagerProtocol | InfraStorage |
| memoRepository | MemoRepositoryProtocol | Domain / Data |
| continuousClock | ContinuousClock | ComposableArchitecture (built-in) |

---

## 6. 実装手順

1. **SearchReducer の State / Action 定義**
2. **TestStoreテスト: searchTextChanged → デバウンス → performSearch** を記述（Red）
3. **Reducer body 実装**: searchTextChanged, performSearch, searchCompleted（Green）
4. **日付フィルターのテスト・実装**
5. **タグフィルターのテスト・実装**
6. **VMSearchBar コンポーネント実装**（設計書セクション4.6）
7. **SearchResultCard コンポーネント実装**（ハイライト付き）
8. **HighlightedSnippetText 実装**（`<mark>` タグのパース）
9. **FilterPanel コンポーネント実装**
10. **SearchView のSwiftUI統合実装**
11. **アクセシビリティ・ダークモード確認**

---

## 7. 設計書参照

| 設計書 | セクション | 内容 |
|:-------|:----------|:-----|
| 01-system-architecture.md | 2.2 TCA適用方針 | SearchReducer のScope構成 |
| 01-system-architecture.md | 5.3 SQLite FTS5 | search(), snippet() の仕様 |
| 04-ui-design-system.md | 4.6 検索バー | VMSearchBar コンポーネント仕様 |
| 04-ui-design-system.md | 6.4 検索画面 | ワイヤーフレーム |
| 04-ui-design-system.md | 5.2 ナビゲーションフロー | 検索→メモ詳細への遷移 |
