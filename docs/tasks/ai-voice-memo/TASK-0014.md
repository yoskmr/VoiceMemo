# TASK-0014: 音声再生 + ハイライト同期

> **タスクID**: TASK-0014
> **フェーズ**: Phase 2 - メモ管理 + 検索
> **タイプ**: TDD
> **見積工数**: 8h
> **依存タスク**: TASK-0012
> **関連要件**: REQ-023
> **ステータス**: TODO
> **担当モジュール**: FeatureMemo, Domain

---

## 1. 概要

AVAudioPlayerによる音声再生機能を実装する。再生/停止/シークバー操作、および再生位置と文字起こしテキストのハイライト同期（許容誤差 ±500ms）を実現する。メモ詳細画面のAudioPlayerPlaceholderを実体のAudioPlayerViewに差し替える。

---

## 2. 完了条件（Definition of Done）

- [ ] AudioPlayerReducer のユニットテスト（TestStore）が全てパス
- [ ] AVAudioPlayerによる音声ファイルの再生/停止が動作する
- [ ] シークバー操作で任意の位置に移動できる
- [ ] 再生中にシークバーがリアルタイムで進行する
- [ ] 経過時間と総時間が「1:23 / 3:24」形式で表示される
- [ ] 再生位置に対応するテキスト範囲がハイライトされる（±500ms許容）
- [ ] 再生終了時に自動的にアイドル状態に戻る
- [ ] バックグラウンド再生はサポートしない（画面離脱で停止）
- [ ] VoiceOverアクセシビリティ対応が完了している

---

## 3. 技術仕様

### 3.1 TCA State / Action / Reducer

```swift
// FeatureMemo/AudioPlayer/AudioPlayerReducer.swift
import ComposableArchitecture
import Foundation

@Reducer
struct AudioPlayerReducer {
    @ObservableState
    struct State: Equatable {
        var audioFilePath: String = ""
        var playerStatus: PlayerStatus = .idle
        var currentTime: TimeInterval = 0
        var duration: TimeInterval = 0
        var progress: Double = 0  // 0.0 - 1.0

        // ハイライト同期
        var timestampedSegments: [TimestampedSegment] = []
        var highlightedSegmentIndex: Int?

        // UI
        var isSliderDragging: Bool = false
        var errorMessage: String?

        enum PlayerStatus: Equatable {
            case idle
            case playing
            case paused
            case finished
        }
    }

    struct TimestampedSegment: Equatable, Identifiable {
        let id: Int  // セグメントインデックス
        let text: String
        let startTime: TimeInterval
        let endTime: TimeInterval
    }

    enum Action {
        case onAppear(audioFilePath: String, segments: [TimestampedSegment])
        case playTapped
        case pauseTapped
        case stopTapped
        case sliderDragStarted
        case sliderDragChanged(Double)  // 0.0 - 1.0
        case sliderDragEnded
        case seekTo(TimeInterval)

        // 内部アクション
        case _playerTimeUpdated(TimeInterval)
        case _playerDidFinish
        case _playerError(String)
        case _playerDurationLoaded(TimeInterval)

        case onDisappear
    }

    @Dependency(\.audioPlayerClient) var audioPlayerClient
    @Dependency(\.continuousClock) var clock

    private enum PlayerTimerID { case timer }

    var body: some ReducerOf<Self> {
        Reduce { state, action in
            switch action {
            case let .onAppear(path, segments):
                state.audioFilePath = path
                state.timestampedSegments = segments
                return .run { send in
                    let duration = try await audioPlayerClient.loadAudio(path)
                    await send(._playerDurationLoaded(duration))
                }

            case let ._playerDurationLoaded(duration):
                state.duration = duration
                return .none

            case .playTapped:
                guard state.playerStatus != .playing else { return .none }
                let seekTime = state.currentTime
                state.playerStatus = .playing
                return .merge(
                    .run { _ in
                        try await audioPlayerClient.play(from: seekTime)
                    },
                    // 再生位置の定期更新（約60fps → 16ms間隔）
                    .run { send in
                        for await _ in clock.timer(interval: .milliseconds(100)) {
                            let time = await audioPlayerClient.currentTime()
                            await send(._playerTimeUpdated(time))
                        }
                    }
                    .cancellable(id: PlayerTimerID.timer)
                )

            case .pauseTapped:
                state.playerStatus = .paused
                return .merge(
                    .run { _ in
                        await audioPlayerClient.pause()
                    },
                    .cancel(id: PlayerTimerID.timer)
                )

            case .stopTapped:
                state.playerStatus = .idle
                state.currentTime = 0
                state.progress = 0
                state.highlightedSegmentIndex = nil
                return .merge(
                    .run { _ in
                        await audioPlayerClient.stop()
                    },
                    .cancel(id: PlayerTimerID.timer)
                )

            case .sliderDragStarted:
                state.isSliderDragging = true
                return .none

            case let .sliderDragChanged(progress):
                state.progress = progress
                state.currentTime = progress * state.duration
                updateHighlight(&state)
                return .none

            case .sliderDragEnded:
                state.isSliderDragging = false
                let seekTime = state.currentTime
                return .run { _ in
                    try await audioPlayerClient.seek(to: seekTime)
                }

            case let .seekTo(time):
                state.currentTime = time
                state.progress = state.duration > 0 ? time / state.duration : 0
                updateHighlight(&state)
                return .run { _ in
                    try await audioPlayerClient.seek(to: time)
                }

            case let ._playerTimeUpdated(time):
                guard !state.isSliderDragging else { return .none }
                state.currentTime = time
                state.progress = state.duration > 0 ? time / state.duration : 0
                updateHighlight(&state)
                return .none

            case ._playerDidFinish:
                state.playerStatus = .finished
                state.highlightedSegmentIndex = nil
                return .cancel(id: PlayerTimerID.timer)

            case let ._playerError(message):
                state.errorMessage = message
                state.playerStatus = .idle
                return .cancel(id: PlayerTimerID.timer)

            case .onDisappear:
                state.playerStatus = .idle
                return .merge(
                    .run { _ in await audioPlayerClient.stop() },
                    .cancel(id: PlayerTimerID.timer)
                )
            }
        }
    }

    /// ハイライト同期: 現在再生位置に対応するセグメントを特定（±500ms許容）
    private func updateHighlight(_ state: inout State) {
        let tolerance: TimeInterval = 0.5  // REQ-023: ±500ms許容
        let currentTime = state.currentTime

        state.highlightedSegmentIndex = state.timestampedSegments.firstIndex { segment in
            currentTime >= (segment.startTime - tolerance) &&
            currentTime <= (segment.endTime + tolerance)
        }
    }
}
```

### 3.2 AudioPlayerClient（Dependency）

```swift
// Domain/Protocols/AudioPlayerClient.swift
import Dependencies

struct AudioPlayerClient {
    var loadAudio: @Sendable (String) async throws -> TimeInterval  // duration
    var play: @Sendable (TimeInterval) async throws -> Void         // from time
    var pause: @Sendable () async -> Void
    var stop: @Sendable () async -> Void
    var seek: @Sendable (TimeInterval) async throws -> Void
    var currentTime: @Sendable () async -> TimeInterval
}

extension AudioPlayerClient: DependencyKey {
    static let liveValue: Self = {
        let player = AudioPlayerActor()
        return Self(
            loadAudio: { path in try await player.load(path: path) },
            play: { from in try await player.play(from: from) },
            pause: { await player.pause() },
            stop: { await player.stop() },
            seek: { time in try await player.seek(to: time) },
            currentTime: { await player.currentTime() }
        )
    }()

    static let testValue: Self = Self(
        loadAudio: { _ in 180.0 },
        play: { _ in },
        pause: { },
        stop: { },
        seek: { _ in },
        currentTime: { 0 }
    )
}

// AVAudioPlayer のActor ラッパー
actor AudioPlayerActor {
    private var player: AVAudioPlayer?

    func load(path: String) throws -> TimeInterval {
        let documentsURL = FileManager.default.urls(
            for: .documentDirectory, in: .userDomainMask
        ).first!
        let fileURL = documentsURL.appendingPathComponent(path)
        player = try AVAudioPlayer(contentsOf: fileURL)
        player?.prepareToPlay()
        return player?.duration ?? 0
    }

    func play(from time: TimeInterval) throws {
        player?.currentTime = time
        player?.play()
    }

    func pause() { player?.pause() }
    func stop() { player?.stop(); player?.currentTime = 0 }

    func seek(to time: TimeInterval) throws {
        player?.currentTime = time
    }

    func currentTime() -> TimeInterval {
        player?.currentTime ?? 0
    }
}
```

### 3.3 AudioPlayerView（SwiftUI）

```swift
// FeatureMemo/AudioPlayer/AudioPlayerView.swift
// 設計書 04-ui-design-system.md セクション6.3 音声プレイヤー部分

struct AudioPlayerView: View {
    @Bindable var store: StoreOf<AudioPlayerReducer>

    var body: some View {
        VStack(spacing: 8) {
            // 再生コントロール + シークバー
            HStack(spacing: 12) {
                // 再生/停止ボタン
                Button {
                    switch store.playerStatus {
                    case .playing:
                        store.send(.pauseTapped)
                    default:
                        store.send(.playTapped)
                    }
                } label: {
                    Image(systemName: store.playerStatus == .playing ? "pause.fill" : "play.fill")
                        .font(.system(size: 20))
                        .foregroundColor(.vmPrimary)
                        .frame(width: 44, height: 44)
                }
                .accessibilityLabel(store.playerStatus == .playing ? "一時停止" : "再生")
                .accessibilityHint("ダブルタップで\(store.playerStatus == .playing ? "一時停止" : "再生開始")します")

                // シークバー
                Slider(
                    value: Binding(
                        get: { store.progress },
                        set: { store.send(.sliderDragChanged($0)) }
                    ),
                    in: 0...1,
                    onEditingChanged: { isDragging in
                        if isDragging {
                            store.send(.sliderDragStarted)
                        } else {
                            store.send(.sliderDragEnded)
                        }
                    }
                )
                .tint(.vmPrimary)
                .accessibilityLabel("再生位置")
                .accessibilityValue("\(formattedTime(store.currentTime)) / \(formattedTime(store.duration))")

                // 時間表示
                Text("\(formattedTime(store.currentTime)) / \(formattedTime(store.duration))")
                    .font(.vmCaption1)
                    .foregroundColor(.vmTextSecondary)
                    .monospacedDigit()
            }
            .padding(.horizontal, 12)
            .padding(.vertical, 8)
            .background(Color.vmSurfaceVariant)
            .cornerRadius(12)
        }
    }

    private func formattedTime(_ time: TimeInterval) -> String {
        let minutes = Int(time) / 60
        let seconds = Int(time) % 60
        return String(format: "%d:%02d", minutes, seconds)
    }
}
```

### 3.4 ハイライト付きテキスト表示

```swift
// FeatureMemo/AudioPlayer/HighlightedTranscriptionView.swift

struct HighlightedTranscriptionView: View {
    let segments: [AudioPlayerReducer.TimestampedSegment]
    let highlightedIndex: Int?
    let onSegmentTapped: (TimeInterval) -> Void

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            // AttributedString でハイライト表現
            ForEach(segments) { segment in
                Text(segment.text)
                    .font(.vmBody())
                    .foregroundColor(.vmTextPrimary)
                    .padding(.vertical, 2)
                    .background(
                        segment.id == highlightedIndex
                            ? Color.vmPrimaryLight.opacity(0.3)
                            : Color.clear
                    )
                    .cornerRadius(4)
                    .onTapGesture {
                        onSegmentTapped(segment.startTime)
                    }
                    .animation(.easeInOut(duration: 0.2), value: highlightedIndex)
            }
        }
        .textSelection(.enabled)
    }
}
```

---

## 4. TDDテスト計画

### 4.1 Red-Green-Refactor サイクル

| # | テストケース | 検証内容 |
|:--|:------------|:---------|
| 1 | `test_onAppear_オーディオファイルロード` | onAppear→loadAudio→durationが設定される |
| 2 | `test_playTapped_再生開始` | playTapped→playerStatus=.playing |
| 3 | `test_pauseTapped_一時停止` | pauseTapped→playerStatus=.paused |
| 4 | `test_stopTapped_停止とリセット` | stopTapped→currentTime=0, progress=0, playerStatus=.idle |
| 5 | `test_playerTimeUpdated_進行率更新` | _playerTimeUpdated(90)→progress=0.5（duration=180の場合） |
| 6 | `test_sliderDrag_ドラッグ中は時間更新を無視` | sliderDragStarted→_playerTimeUpdated→progress変化なし |
| 7 | `test_sliderDragEnded_シーク実行` | sliderDragEnded→seek(to:)が呼ばれる |
| 8 | `test_highlightSync_正しいセグメントがハイライト` | currentTime=30→startTime=28, endTime=35のセグメントがハイライト |
| 9 | `test_highlightSync_許容誤差500ms` | currentTime=27.5（startTime=28-0.5）→ハイライトされる |
| 10 | `test_highlightSync_範囲外でハイライトなし` | currentTime=100でセグメント外→highlightedSegmentIndex=nil |
| 11 | `test_playerDidFinish_再生終了` | _playerDidFinish→playerStatus=.finished、タイマーキャンセル |
| 12 | `test_onDisappear_再生停止` | onDisappear→stop()が呼ばれ、タイマーキャンセル |
| 13 | `test_seekTo_指定時間にシーク` | seekTo(60)→currentTime=60, progress更新、ハイライト更新 |

### 4.2 TestStore テスト例

```swift
@MainActor
func test_highlightSync_正しいセグメントがハイライト() async {
    let segments: [AudioPlayerReducer.TimestampedSegment] = [
        .init(id: 0, text: "最初のセグメント", startTime: 0, endTime: 15),
        .init(id: 1, text: "次のセグメント", startTime: 15, endTime: 30),
        .init(id: 2, text: "最後のセグメント", startTime: 30, endTime: 45),
    ]

    let store = TestStore(
        initialState: AudioPlayerReducer.State(duration: 45)
    ) {
        AudioPlayerReducer()
    } withDependencies: {
        $0.audioPlayerClient = .testValue
        $0.continuousClock = ImmediateClock()
    }

    // セグメント1（15-30秒）の中間地点
    await store.send(._playerTimeUpdated(22.0)) {
        $0.currentTime = 22.0
        $0.progress = 22.0 / 45.0
        $0.highlightedSegmentIndex = 1  // 2番目のセグメント
    }
}
```

---

## 5. 依存関係

### 5.1 前提タスク

| タスクID | 内容 | 必要な成果物 |
|:---------|:-----|:------------|
| TASK-0012 | メモ詳細画面 | AudioPlayerPlaceholder を差し替える画面構造 |

### 5.2 後続タスク

なし（独立した再生機能）

### 5.3 @Dependency

| 依存名 | プロトコル | 提供モジュール |
|:-------|:----------|:--------------|
| audioPlayerClient | AudioPlayerClient | Domain / FeatureMemo |
| continuousClock | ContinuousClock | ComposableArchitecture (built-in) |

---

## 6. 実装手順

1. **AudioPlayerClient の依存定義**（protocol + testValue）
2. **AudioPlayerReducer の State / Action 定義**
3. **TestStoreテスト: play / pause / stop** を記述（Red）
4. **Reducer body 実装**: 基本再生コントロール（Green）
5. **シークバー操作のテスト・実装**
6. **ハイライト同期ロジックのテスト・実装**（±500ms許容）
7. **AudioPlayerView のSwiftUI実装**
8. **HighlightedTranscriptionView のSwiftUI実装**
9. **AudioPlayerActor（AVAudioPlayerラッパー）の実装**
10. **MemoDetailViewへの統合**（Placeholder差し替え）
11. **アクセシビリティ確認**

---

## 7. 設計書参照

| 設計書 | セクション | 内容 |
|:-------|:----------|:-----|
| 01-system-architecture.md | 6.1 ディレクトリ構成 | Documents/Audio/ に音声ファイル保存 |
| 01-system-architecture.md | 6.2 AudioFileStore | 音声ファイルパスの解決方法 |
| 04-ui-design-system.md | 6.3 メモ詳細画面 | 音声プレイヤー部分のワイヤーフレーム |
| 04-ui-design-system.md | 8.1 VoiceOver対応 | 波形ビジュアライザのアクセシビリティ |
