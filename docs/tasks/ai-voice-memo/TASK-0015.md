# TASK-0015: SQLite FTS5全文検索エンジン

> **タスクID**: TASK-0015
> **フェーズ**: Phase 2 - メモ管理 + 検索
> **タイプ**: TDD
> **見積工数**: 8h
> **依存タスク**: TASK-0002
> **関連要件**: REQ-006
> **ステータス**: TODO
> **担当モジュール**: InfraStorage

---

## 1. 概要

SQLite FTS5仮想テーブルを使用した全文検索エンジンを実装する。ICUトークナイザ（日本語形態素解析）を一次候補とし、unicode61 + trigram をフォールバックとする。インデックスの作成・更新・削除、日本語全文検索クエリの実行、スニペット抽出を実装する。

---

## 2. 完了条件（Definition of Done）

- [ ] FTS5IndexManager のユニットテストが全てパス
- [ ] FTS5仮想テーブル（memo_fts）が正しく作成される
- [ ] ICUトークナイザ利用可否の自動判定が動作する
- [ ] ICU非対応時にunicode61 + trigram にフォールバックする
- [ ] インデックスの追加（upsertIndex）が動作する
- [ ] インデックスの更新（既存レコードのDELETE + INSERT）が動作する
- [ ] インデックスの削除（removeIndex）が動作する
- [ ] 日本語全文検索クエリが正しく動作する
- [ ] スニペット抽出（検索キーワード前後のコンテキスト取得）が動作する
- [ ] 検索結果がランク順（relevance順）で返される
- [ ] 1,000件インデックスでの検索が1秒以内（NFR-005相当）

---

## 3. 技術仕様

### 3.1 FTS5テーブル設計

設計書 01-system-architecture.md セクション5.3 準拠。

```sql
-- ICUトークナイザ使用時（一次候補）
CREATE VIRTUAL TABLE IF NOT EXISTS memo_fts USING fts5(
    memo_id UNINDEXED,
    title,
    transcription_text,
    summary_text,
    tags,
    tokenize = 'icu ja_JP'
);

-- unicode61 + trigram フォールバック（ICU非対応環境）
CREATE VIRTUAL TABLE IF NOT EXISTS memo_fts USING fts5(
    memo_id UNINDEXED,
    title,
    transcription_text,
    summary_text,
    tags,
    tokenize = 'unicode61',
    content_rowid = 'rowid'
);

CREATE VIRTUAL TABLE IF NOT EXISTS memo_fts_trigram USING fts5(
    memo_id UNINDEXED,
    title,
    transcription_text,
    tokenize = 'trigram'
);
```

### 3.2 FTS5IndexManager 実装

```swift
// InfraStorage/FTS5/FTS5IndexManager.swift
import SQLite3
import Foundation

/// FTS5全文検索インデックスの管理
/// REQ-006: フルテキスト検索
/// 設計書 01-system-architecture.md セクション5.3 準拠
final class FTS5IndexManager: Sendable {

    private let dbPath: String
    private let useICU: Bool

    init(dbPath: String) {
        self.dbPath = dbPath
        self.useICU = FTS5IndexManager.checkICUAvailability(dbPath: dbPath)
    }

    // MARK: - テーブル作成

    /// FTS5仮想テーブルの作成
    func createIndex() throws {
        if useICU {
            try execute(sql: """
                CREATE VIRTUAL TABLE IF NOT EXISTS memo_fts USING fts5(
                    memo_id UNINDEXED,
                    title,
                    transcription_text,
                    summary_text,
                    tags,
                    tokenize = 'icu ja_JP'
                );
            """)
        } else {
            // unicode61 フォールバック
            try execute(sql: """
                CREATE VIRTUAL TABLE IF NOT EXISTS memo_fts USING fts5(
                    memo_id UNINDEXED,
                    title,
                    transcription_text,
                    summary_text,
                    tags,
                    tokenize = 'unicode61'
                );
            """)
            // trigram テーブル（部分一致検索用）
            try execute(sql: """
                CREATE VIRTUAL TABLE IF NOT EXISTS memo_fts_trigram USING fts5(
                    memo_id UNINDEXED,
                    title,
                    transcription_text,
                    tokenize = 'trigram'
                );
            """)
        }
    }

    // MARK: - CRUD操作

    /// メモのインデックス追加・更新（DELETE + INSERT方式）
    func upsertIndex(
        memoID: String,
        title: String,
        transcriptionText: String,
        summaryText: String,
        tags: String
    ) throws {
        // 既存レコードを削除
        try execute(
            sql: "DELETE FROM memo_fts WHERE memo_id = ?;",
            params: [memoID]
        )

        // 新規挿入
        try execute(
            sql: """
                INSERT INTO memo_fts (memo_id, title, transcription_text, summary_text, tags)
                VALUES (?, ?, ?, ?, ?);
            """,
            params: [memoID, title, transcriptionText, summaryText, tags]
        )

        // trigram テーブルも更新（フォールバック環境のみ）
        if !useICU {
            try execute(
                sql: "DELETE FROM memo_fts_trigram WHERE memo_id = ?;",
                params: [memoID]
            )
            try execute(
                sql: """
                    INSERT INTO memo_fts_trigram (memo_id, title, transcription_text)
                    VALUES (?, ?, ?);
                """,
                params: [memoID, title, transcriptionText]
            )
        }
    }

    /// インデックスからメモを削除
    func removeIndex(memoID: String) throws {
        try execute(
            sql: "DELETE FROM memo_fts WHERE memo_id = ?;",
            params: [memoID]
        )
        if !useICU {
            try execute(
                sql: "DELETE FROM memo_fts_trigram WHERE memo_id = ?;",
                params: [memoID]
            )
        }
    }

    // MARK: - 検索

    /// フルテキスト検索の実行
    /// - Parameter query: 検索クエリ（日本語対応）
    /// - Returns: マッチしたメモIDの配列（ランク順）
    func search(query: String) throws -> [SearchResult] {
        let sanitizedQuery = sanitizeQuery(query)
        guard !sanitizedQuery.isEmpty else { return [] }

        let results = try executeQuery(
            sql: """
                SELECT memo_id,
                       snippet(memo_fts, 2, '<mark>', '</mark>', '...', 32) AS snippet,
                       rank
                FROM memo_fts
                WHERE memo_fts MATCH ?
                ORDER BY rank;
            """,
            params: [sanitizedQuery]
        )

        // ICU非対応環境ではtrigramテーブルも検索してマージ
        if !useICU {
            let trigramResults = try executeQuery(
                sql: """
                    SELECT memo_id,
                           snippet(memo_fts_trigram, 2, '<mark>', '</mark>', '...', 32) AS snippet,
                           rank
                    FROM memo_fts_trigram
                    WHERE memo_fts_trigram MATCH ?
                    ORDER BY rank;
                """,
                params: [sanitizedQuery]
            )
            return mergeResults(primary: results, secondary: trigramResults)
        }

        return results
    }

    /// スニペット抽出（検索キーワード前後のコンテキスト）
    func searchWithSnippets(
        query: String,
        snippetColumn: Int = 2,  // transcription_text
        maxTokens: Int = 32
    ) throws -> [SearchResult] {
        let sanitizedQuery = sanitizeQuery(query)
        guard !sanitizedQuery.isEmpty else { return [] }

        return try executeQuery(
            sql: """
                SELECT memo_id,
                       snippet(memo_fts, ?, '<mark>', '</mark>', '...', ?) AS snippet,
                       rank
                FROM memo_fts
                WHERE memo_fts MATCH ?
                ORDER BY rank;
            """,
            params: [String(snippetColumn), String(maxTokens), sanitizedQuery]
        )
    }

    // MARK: - ヘルパー

    struct SearchResult: Equatable {
        let memoID: String
        let snippet: String
        let rank: Double
    }

    /// 検索クエリのサニタイズ（FTS5構文エスケープ）
    private func sanitizeQuery(_ query: String) -> String {
        // FTS5特殊文字をエスケープ
        let specialChars = CharacterSet(charactersIn: "\"*():-^")
        let escaped = query.unicodeScalars.filter { !specialChars.contains($0) }
        let cleaned = String(String.UnicodeScalarView(escaped)).trimmingCharacters(in: .whitespaces)

        // 空白区切りをAND検索に変換
        let terms = cleaned.split(separator: " ").map { "\"\($0)\"" }
        return terms.joined(separator: " AND ")
    }

    /// ICUトークナイザの利用可否チェック
    private static func checkICUAvailability(dbPath: String) -> Bool {
        var db: OpaquePointer?
        guard sqlite3_open(dbPath, &db) == SQLITE_OK else { return false }
        defer { sqlite3_close(db) }

        let testSQL = "CREATE VIRTUAL TABLE IF NOT EXISTS _icu_test USING fts5(test_col, tokenize = 'icu ja_JP');"
        let result = sqlite3_exec(db, testSQL, nil, nil, nil)
        sqlite3_exec(db, "DROP TABLE IF EXISTS _icu_test;", nil, nil, nil)
        return result == SQLITE_OK
    }

    /// 検索結果のマージ（重複除去、ランク順維持）
    private func mergeResults(primary: [SearchResult], secondary: [SearchResult]) -> [SearchResult] {
        var seen = Set(primary.map(\.memoID))
        var merged = primary
        for result in secondary where !seen.contains(result.memoID) {
            seen.insert(result.memoID)
            merged.append(result)
        }
        return merged
    }

    // MARK: - SQLite3 C API ラッパー

    private func execute(sql: String, params: [String] = []) throws {
        // SQLite3 C APIを使用した実行
        // ... (省略、実装時に詳細化)
    }

    private func executeQuery(sql: String, params: [String]) throws -> [SearchResult] {
        // SQLite3 C APIを使用したクエリ実行
        // ... (省略、実装時に詳細化)
    }
}
```

### 3.3 FTS5IndexManagerProtocol（Dependency注入用）

```swift
// Domain/Protocols/FTS5IndexManagerProtocol.swift

protocol FTS5IndexManagerProtocol: Sendable {
    func createIndex() throws
    func upsertIndex(memoID: String, title: String, transcriptionText: String, summaryText: String, tags: String) throws
    func removeIndex(memoID: String) throws
    func search(query: String) throws -> [FTS5SearchResult]
    func searchWithSnippets(query: String, snippetColumn: Int, maxTokens: Int) throws -> [FTS5SearchResult]
}

struct FTS5SearchResult: Equatable, Sendable {
    let memoID: String
    let snippet: String
    let rank: Double
}

// TCA Dependency 登録
extension DependencyValues {
    var fts5IndexManager: any FTS5IndexManagerProtocol {
        get { self[FTS5IndexManagerKey.self] }
        set { self[FTS5IndexManagerKey.self] = newValue }
    }
}
```

### 3.4 設計判断サマリー

| 項目 | 設計判断 | 理由 | 設計書参照 |
|:-----|:---------|:-----|:-----------|
| トークナイザ | ICU ja_JP（一次）/ unicode61 + trigram（フォールバック） | 日本語形態素解析対応。ICU非対応環境のフォールバック | 01-arch セクション5.3 |
| インデックス対象 | title, transcription_text, summary_text, tags | 検索対象テキスト系フィールド全て | 01-arch セクション5.3 |
| memo_id | UNINDEXED | 結合キーのみ、検索対象外 | 01-arch セクション5.3 |
| 更新方式 | DELETE + INSERT | FTS5はUPDATEの性能が劣る | 01-arch セクション5.3 |
| スニペット | snippet()関数、mark タグ | 検索結果のハイライト表示用 | REQ-006 |

---

## 4. TDDテスト計画

### 4.1 Red-Green-Refactor サイクル

| # | テストケース | 検証内容 |
|:--|:------------|:---------|
| 1 | `test_createIndex_テーブル作成成功` | FTS5テーブルが作成され、存在確認できる |
| 2 | `test_createIndex_ICU非対応環境でフォールバック` | ICU非対応→unicode61 + trigram テーブルが作成される |
| 3 | `test_upsertIndex_新規追加` | upsertIndex→search で該当メモが見つかる |
| 4 | `test_upsertIndex_既存更新` | 同一memoIDで2回upsert→検索結果は1件 |
| 5 | `test_removeIndex_削除後に検索されない` | removeIndex→search で該当メモが見つからない |
| 6 | `test_search_日本語クエリ` | 「アイデア」で検索→該当メモが返される |
| 7 | `test_search_複数キーワードAND検索` | 「アプリ 開発」→両方含むメモのみ返される |
| 8 | `test_search_ランク順` | 複数マッチ時にrelevance順で返される |
| 9 | `test_search_該当なし` | 存在しないキーワード→空配列 |
| 10 | `test_search_空クエリ` | 空文字→空配列（エラーなし） |
| 11 | `test_searchWithSnippets_スニペット抽出` | 検索結果にmarkタグ付きスニペットが含まれる |
| 12 | `test_sanitizeQuery_特殊文字エスケープ` | FTS5特殊文字が正しくエスケープされる |
| 13 | `test_performance_1000件検索` | 1,000件インデックスでの検索が1秒以内 |

### 4.2 テスト例

```swift
func test_search_日本語クエリ() throws {
    let sut = FTS5IndexManager(dbPath: testDBPath)
    try sut.createIndex()

    try sut.upsertIndex(
        memoID: "memo-1",
        title: "通勤中のアイデア",
        transcriptionText: "今日のアプリ開発のアイデアを思いついた",
        summaryText: "アプリのアイデアメモ",
        tags: "アイデア,アプリ開発"
    )

    try sut.upsertIndex(
        memoID: "memo-2",
        title: "買い物リスト",
        transcriptionText: "牛乳と卵を買う",
        summaryText: "買い物メモ",
        tags: "買い物"
    )

    let results = try sut.search(query: "アイデア")

    XCTAssertEqual(results.count, 1)
    XCTAssertEqual(results[0].memoID, "memo-1")
}

func test_performance_1000件検索() throws {
    let sut = FTS5IndexManager(dbPath: testDBPath)
    try sut.createIndex()

    // 1,000件のインデックスを作成
    for i in 0..<1000 {
        try sut.upsertIndex(
            memoID: "memo-\(i)",
            title: "メモ\(i)",
            transcriptionText: "テスト文字起こしテキスト\(i) \(i % 10 == 0 ? "特別なキーワード" : "")",
            summaryText: "要約\(i)",
            tags: "タグ\(i % 5)"
        )
    }

    measure {
        _ = try? sut.search(query: "特別なキーワード")
    }
}
```

---

## 5. 依存関係

### 5.1 前提タスク

| タスクID | 内容 | 必要な成果物 |
|:---------|:-----|:------------|
| TASK-0002 | SwiftDataストア構成 | SQLiteデータベースファイルパス |

### 5.2 後続タスク

| タスクID | 内容 | 本タスクの提供物 |
|:---------|:-----|:----------------|
| TASK-0016 | 検索UI画面 | FTS5IndexManagerProtocol.search() |
| TASK-0017 | メモ削除 | FTS5IndexManagerProtocol.removeIndex() |
| TASK-0013 | メモテキスト編集 | FTS5IndexManagerProtocol.upsertIndex()（テキスト更新時） |

### 5.3 外部依存

| ライブラリ | 用途 |
|:----------|:-----|
| SQLite3 (system) | FTS5仮想テーブルのC API |

---

## 6. 実装手順

1. **FTS5IndexManagerProtocol の定義**（Domain層）
2. **テスト: createIndex / ICUチェック** を記述（Red）
3. **FTS5IndexManager.createIndex() 実装**: ICU判定 + テーブル作成（Green）
4. **テスト: upsertIndex / removeIndex** を記述（Red）
5. **CRUD操作の実装**（Green）
6. **テスト: search（日本語クエリ）** を記述（Red）
7. **検索クエリ実行 + サニタイズの実装**（Green）
8. **スニペット抽出の実装**
9. **フォールバック環境（unicode61 + trigram）のテスト・実装**
10. **パフォーマンステスト（1,000件）**
11. **TCA @Dependency 登録**

---

## 7. 設計書参照

| 設計書 | セクション | 内容 |
|:-------|:----------|:-----|
| 01-system-architecture.md | 5.3 SQLite FTS5 インデックス設計 | テーブル定義、トークナイザ選択、設計判断 |
| 01-system-architecture.md | 6.1 ディレクトリ構成 | SecureStore/default.sqlite のパス |
| 01-system-architecture.md | 6.3 SwiftData ストア構成 | ModelContainerConfiguration |
| 01-system-architecture.md | 7.1 パフォーマンス NFR-005 | 1,000件一覧 1秒以内 |
